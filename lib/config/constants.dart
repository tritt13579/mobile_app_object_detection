import 'package:camera/camera.dart';

class AppConstants {
  // Camera settings
  static const ResolutionPreset cameraResolution = ResolutionPreset.medium;
  static const bool enableAudio = false;
  static const ImageFormatGroup imageFormat = ImageFormatGroup.jpeg;

  // UI settings
  static const String appTitle = "Camera";
  static const String homeTitle = "Object Detection App";
}